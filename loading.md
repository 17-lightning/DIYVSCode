# 待办事项
+ DIY跳转功能中，两份配置文件现在是互斥的，其实应该让它们同时存在时就同时生效，封装一下函数应该就行

# 更新记录

## 20250701
+ 更换了`DIY-jump.md`配置文件的读取顺序，现在将优先读取当前工作区的配置，再读取vscode里的配置
+ 更改了寻址规则，新的寻址规则如下：以@开头的在当前工作区寻址，以.开头的以相对地址寻址，以#开头的去配置里寻址
